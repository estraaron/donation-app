import { Routes } from '@angular/router';
import { AppComponent } from './app.component';

export const routes: Routes = [
  { path: 'donation-form', component: AppComponent },
  // { path: '**', redirectTo: 'donation-form' },
];
